journey:
  id: no-payments
  name: No Outstanding Payments
  tags: [parent, authenticated, payments, edge]
  role: parent
  preconditions:
    seed: parent-with-school
    state: authenticated
  steps:
    - action: navigate
      target: payments
      selectors:
        web: "/dashboard/payments"
        mobile: "Payments"
  assertions:
    - type: visible
      text: "No payments"
      mobileText: "No payments"
