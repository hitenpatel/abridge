appId: com.abridge.app
name: Staff Visitors
tags:
  - staff
  - visitors
---
- runFlow: ../_helpers/login-staff.yaml

# Navigate to visitors via dev nav button
- tapOn:
    id: "nav-visitors"

# Verify visitors screen loaded (title or feature-disabled message)
- extendedWaitUntil:
    visible: "Visitors"
    timeout: 60000
