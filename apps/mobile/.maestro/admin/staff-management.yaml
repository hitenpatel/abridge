appId: com.abridge.app
name: Admin Staff Management
tags:
  - admin
  - staff
---
- runFlow: ../_helpers/login-staff.yaml

# Verify staff home loads (Staff Management requires admin role
# which may not populate in session immediately on CI)
- assertVisible:
    text: "Home"
- assertVisible: "Post Update"
