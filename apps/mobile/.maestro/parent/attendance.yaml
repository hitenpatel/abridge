appId: com.abridge.app
name: Parent Attendance
tags:
  - parent
  - attendance
---
- runFlow: ../_helpers/login-parent.yaml

# Navigate to attendance via dev nav button
- tapOn:
    id: "nav-attendance"
- extendedWaitUntil:
    visible: "Attendance Hub"
    timeout: 10000

# Verify absence reporting section
- assertVisible: "Report Absence"
- assertVisible: "Sick"
- assertVisible: "Appointment"
- assertVisible: "Family"
- assertVisible: "Other"

# Verify recent records section
- assertVisible: "Recent Records"
