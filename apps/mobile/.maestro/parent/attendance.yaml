appId: com.abridge.app
name: Parent Attendance
tags:
  - parent
  - attendance
---
- runFlow: ../_helpers/login-parent.yaml

# Navigate to attendance via dev nav button
- tapOn:
    id: "nav-attendance"
- extendedWaitUntil:
    visible: "Attendance Hub"
    timeout: 60000
- takeScreenshot: native-parent-attendance

# Verify absence reporting section
- extendedWaitUntil:
    visible: "Report Absence"
    timeout: 60000
- extendedWaitUntil:
    visible: "Sick"
    timeout: 60000
- extendedWaitUntil:
    visible: "Appointment"
    timeout: 60000
- extendedWaitUntil:
    visible: "Family"
    timeout: 60000
- extendedWaitUntil:
    visible: "Other"
    timeout: 60000

# Verify recent records section (below fold, need to scroll)
- scrollUntilVisible:
    element: "Recent Records"
    direction: DOWN
    timeout: 60000
