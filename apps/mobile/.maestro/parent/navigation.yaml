appId: com.abridge.app
name: Parent Tab Navigation
tags:
  - parent
  - navigation
---
- runFlow: ../_helpers/login-parent.yaml

# Navigate to Messages via dev nav button (tab bar taps don't work on iOS)
- tapOn:
    id: "nav-messages"
- extendedWaitUntil:
    visible: "Inbox"
    timeout: 60000
